#include "main.h"
/**
 * add -function that adds two integers and returns the result
 * @a: input value
 * @b: input value
 * Return: results
 */
int add(int a, int b)
{
int result = a + b;
return (result);
}
